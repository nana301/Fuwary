<div class="max-w-2xl mx-auto px-4 py-10">
  <h1 class="text-3xl font-bold text-center mb-6">
    🔮 今日の運勢
  </h1>

  <!-- メッセージ -->
  <div class="bg-white rounded-2xl shadow p-6 mb-6">
    <p class="text-gray-800 leading-relaxed text-center">
      <%= @tarot_result.result_text %>
    </p>
  </div>

  <!-- カード表示 -->
  <div class="flex justify-center mb-8">
    <% card = @result_cards.first %>
    <div class="w-40 text-center">
      <div class="bg-purple-100 rounded-xl p-4 shadow">
        <p class="font-semibold text-purple-700 mb-2">
          <%= card.name %>
        </p>
        <p class="text-sm text-gray-600">
          <%= card.meaning %>
        </p>
      </div>
    </div>
  </div>

  <!-- CTA -->
  <div class="bg-purple-50 border border-purple-200 rounded-2xl p-6 text-center">
    <p class="mb-4 text-gray-700">
      ログインすると<br>
      <span class="font-semibold">ジャンル別・感情占い</span>や<br>
      <span class="font-semibold">カードを追加で引く</span>ことができます✨
    </p>

    <div class="flex flex-col gap-3">
      <%= link_to "ログインして占いを続ける",
                  new_user_session_path,
                  class: "w-full py-3 bg-purple-600 text-white rounded-xl hover:bg-purple-700" %>

      <%= link_to "新規登録はこちら",
                  new_user_registration_path,
                  class: "w-full py-3 border border-purple-400 text-purple-700 rounded-xl hover:bg-purple-100" %>
    </div>
  </div>

  <!-- 戻る -->
  <div class="text-center mt-6">
    <%= link_to "トップに戻る", root_path, class: "text-sm text-gray-500 underline" %>
  </div>
</div>
