<% if tarot_result.can_draw_more? %>
  <div class="card text-center">
    <p class="text-sm text-gray-600 mb-4">もう1枚引いて、メッセージを深めましょう</p>

    <%= form_with url: draw_tarot_result_path(tarot_result),
      method: :post do %>
      <button type="submit" class="btn-primary w-full py-4 text-lg">
        カードを引く
      </button>
    <% end %>

    <p class="text-xs text-gray-500 mt-3">最大3枚まで引けます</p>
  </div>
<% else %>
  <div class="card text-center">
    <p class="text-sm font-semibold text-gray-800">3枚引き終わりました</p>
    <p class="text-xs text-gray-500 mt-1">結果をゆっくり読み返してみてください</p>
  </div>
<% end %>
